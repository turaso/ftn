#include <ftn/output.hh>

namespace ftn {

// std::string string(const Fraction& f) {
  // return std::to_string(f.numerator()) + " / " + std::to_string(f.denominator());
// }

// std::string string(const Fraction f) {
//   // return string(f);
//   return std::to_string(f.numerator()) + " / " + std::to_string(f.denominator());
// }

} /// namespace ftn
